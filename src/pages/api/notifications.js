import React from 'react';

import Sidebar from '../../components/Sidebar/Sidebar';

const notifications = () => {
    return (
        <div>
            <Sidebar></Sidebar>
            <h1>notifications</h1>
        </div>
    );
};

export default notifications;